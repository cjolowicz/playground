name: Tests
on: [push]
jobs:
  tests:
    runs-on: windows-latest
    steps:
      - uses: actions/setup-python@latest
      - shell: python
        run: |
          from pathlib import Path
          print(Path("relative").resolve())
